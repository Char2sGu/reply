import { DirectionalScrollEventsDirective } from './directional-scroll-events.directive';

describe('DirectionalScrollEventsDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectionalScrollEventsDirective();
    expect(directive).toBeTruthy();
  });
});
