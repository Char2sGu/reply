import { ScrollStateDirective } from './scrolling-state.directive';

describe('ScrollStateDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollStateDirective();
    expect(directive).toBeTruthy();
  });
});
