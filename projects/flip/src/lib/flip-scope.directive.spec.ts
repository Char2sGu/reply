import { FlipScopeDirective } from './flip-scope.directive';

describe('FlipScopeDirective', () => {
  it('should create an instance', () => {
    const directive = new FlipScopeDirective();
    expect(directive).toBeTruthy();
  });
});
