import { FlipItemDirective } from './flip-item.directive';

describe('FlipItemDirective', () => {
  it('should create an instance', () => {
    const directive = new FlipItemDirective();
    expect(directive).toBeTruthy();
  });
});
