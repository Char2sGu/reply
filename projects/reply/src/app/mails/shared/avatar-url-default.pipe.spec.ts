import { AvatarUrlDefaultPipe } from './avatar-url-default.pipe';

describe('AvatarUrlDefaultPipe', () => {
  it('create an instance', () => {
    const pipe = new AvatarUrlDefaultPipe();
    expect(pipe).toBeTruthy();
  });
});
